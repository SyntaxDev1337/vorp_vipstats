It is a system inspired by Counter-Strike 2, created for RedM. You need to connect the arena system to the script + you need to create a folder for the index named "html" + you need to import the SQL from the folder into your database.
